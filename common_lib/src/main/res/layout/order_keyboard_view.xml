<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/order_keyboard_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:visibility="gone">

    <com.decard.mvpframe.widgets.OrderKeyBoard
        android:id="@+id/order_keyboard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/color_key_line"
        android:keyBackground="@color/color_key_background"
        android:keyTextColor="@android:color/black"
        android:shadowRadius="0.0"
        android:keyTextSize="25sp" />

</RelativeLayout>
